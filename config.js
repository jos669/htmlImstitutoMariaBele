// Config del frontend estático (GitHub Pages)
// URL base de la API Flask. Sin barra final. Vacío = mismo origen (dev local).
window.API_BASE = '';
// Rutas de navegación interna del site estático (relativas a la raíz del site).
window.APP_PATHS = {
  landing: './index.html',
  portal: './portal.html',
  adminLogin: './admin_login.html',
  admin: './admin.html'
};
